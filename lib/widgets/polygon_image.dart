import 'package:flutter/material.dart';

class PolygonImage extends StatelessWidget {
  const PolygonImage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Image.asset(
      "assets/polygon_.png",
    );
  }
}
